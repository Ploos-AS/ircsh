# M6 account-store hardening\n\nThe administrator account registry now rejects symlinked store, directory and lock paths; serializes read-modify-write operations with an advisory exclusive lock; preserves `0700` directory and `0600` file/lock modes; and fsyncs the parent directory after atomic replacement. This closes the previously identified symlink, durability and concurrent-writer gaps.\n