# M4 quota measurement hardening\n\nDisk accounting uses `lstat()` so symlink targets outside the quota root are not charged or traversed. Standard filesystem traversal is requested with `followlinks=False`, and walker errors fail closed. Linux TCP accounting now treats malformed `/proc/net/tcp*` rows as measurement failures instead of silently under-counting, and counts active connection states rather than ESTABLISHED only.\n